"""机型提取与问题改写提示词。"""

# 系统角色：售后客服助手，负责“理解意图 + 提取关键信息”
MODEL_EXTRACT_SYSTEM_PROMPT = "你是一个专业的 3D 打印机售后客服助手，擅长理解用户意图并提取关键信息。"

# 用户模板：调用方填充 history_text（历史会话）与 query（当前问题）
MODEL_EXTRACT_TEMPLATE = """
历史会话：
{history_text}

当前用户问题：
{query}

请根据历史会话和当前问题，提取用户正在咨询的打印机机型（models）。
1. 如果用户明确提到了机型，请提取出来；可能有一个或多个，但不能重复。
2. 如果用户使用了代词（如"这个"、"它"），请结合历史会话做指代消解，确定机型。
3. 如果无法确定机型，models 返回空列表。
4. 请改写用户问题（rewritten_query），使其成为包含机型的独立完整问题。

请直接返回 JSON，格式如下：
{{
    "models": ["机型A", "机型B"],
    "rewritten_query": "关于机型A，..."
}}
"""
